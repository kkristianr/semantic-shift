{
  "name": "diachronic-data-backend",
  "version": "0.1.0",
  "description": "FastAPI backend for diachronic data visualization",
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "format": "uv run black .",
    "lint": "uv run flake8 .",
    "sort": "uv run isort .",
    "install": "uv sync",
    "install-dev": "uv sync --dev",
    "add": "uv add",
    "add-dev": "uv add --dev"
  },
  "keywords": ["fastapi", "python", "nlp", "visualization"],
  "author": "Your Name",
  "license": "MIT"
} 